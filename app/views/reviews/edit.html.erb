<h1>Editing review</h1>

<%= render "form", review: @review %>

<br>

<div>
  <%= link_to 'Show', [@book, @review] %> |
  <%= link_to 'Back', book_reviews_path(@book) %>
  <%= link_to 'Delete', [@book, @review],  { turbo_method: :delete, turbo_confirm: 'Are you sure?' } %>
</div>
